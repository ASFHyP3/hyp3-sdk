name: hyp3-sdk
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - pip
  # For packaging, and testing
  - python-build
  - ruff
  - jupyter
  - setuptools>=61
  - setuptools_scm>=6.2
  - pytest
  - pytest-cov
  - responses
  # For running
  - python-dateutil
  - requests
  - tqdm
  - urllib3
